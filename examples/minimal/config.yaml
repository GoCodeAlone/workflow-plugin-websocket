# Minimal config exercising workflow-plugin-websocket.
# Schema-validate with: wfctl validate --skip-unknown-types examples/minimal/config.yaml
version: 1
modules:
  - name: websocket-provider
    type: ws.server
    config: {}
workflows:
  pipeline:
    steps:
      - name: example
        type: step.ws_send
        config: {}
